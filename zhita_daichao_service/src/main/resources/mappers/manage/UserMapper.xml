<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhita.dao.manage.UserMapper">
	<resultMap id="BaseResultMap"
		type="com.zhita.model.manage.User">
		<constructor>
			<idArg column="id" javaType="java.lang.Integer"
				jdbcType="INTEGER" />
			<arg column="sourceId" javaType="java.lang.Integer"
				jdbcType="INTEGER" />
			<arg column="nickname" javaType="java.lang.String"
				jdbcType="VARCHAR" />
			<arg column="phone" javaType="java.lang.String"
				jdbcType="VARCHAR" />
			<arg column="dayFen" javaType="java.lang.Integer"
				jdbcType="INTEGER" />
			<arg column="registrationTime" javaType="java.lang.String"
				jdbcType="VARCHAR" />
			<arg column="name" javaType="java.lang.String" jdbcType="VARCHAR" />
			<arg column="age" javaType="java.lang.Integer" jdbcType="INTEGER" />
			<arg column="idCard" javaType="java.lang.String"
				jdbcType="VARCHAR" />
			<arg column="openId" javaType="java.lang.String"
				jdbcType="VARCHAR" />
		</constructor>
	</resultMap>
	<sql id="Base_Column_List">
		id, sourceId, nickname, phone, dayFen, registrationTime,
		name, age, idCard,openId
	</sql>	
	<delete id="deleteByPrimaryKey"
		parameterType="java.lang.Integer">
		delete from user
		where id = #{id,jdbcType=INTEGER}
	</delete>
	<insert id="insert" parameterType="com.zhita.model.manage.User">
		insert into user (id, sourceId,
		nickname,
		phone, dayFen, registrationTime,
		name, age, idCard,openId)
		values
		(#{id,jdbcType=INTEGER}, #{sourceId,jdbcType=INTEGER},
		#{nickname,jdbcType=VARCHAR},
		#{phone,jdbcType=VARCHAR},
		#{dayfen,jdbcType=INTEGER}, #{registrationtime,jdbcType=VARCHAR},
		#{name,jdbcType=VARCHAR}, #{age,jdbcType=INTEGER},
		#{idcard,jdbcType=VARCHAR},#{openId,jdbcType=VARCHAR})
	</insert>
	<insert id="insertSelective"
		parameterType="com.zhita.model.manage.User">
		insert into user
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="id != null">
				id,
			</if>
			<if test="sourceId != null">
				sourceId,
			</if>
			<if test="nickname != null">
				nickname,
			</if>
			<if test="phone != null">
				phone,
			</if>
			<if test="dayfen != null">
				dayFen,
			</if>
			<if test="registrationtime != null">
				registrationTime,
			</if>
			<if test="name != null">
				name,
			</if>
			<if test="age != null">
				age,
			</if>
			<if test="idcard != null">
				idCard,
			</if>
			<if test="openId != null">
				openId,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="id != null">
				#{id,jdbcType=INTEGER},
			</if>
			<if test="sourceId != null">
				#{sourceId,jdbcType=INTEGER},
			</if>
			<if test="nickname != null">
				#{nickname,jdbcType=VARCHAR},
			</if>
			<if test="phone != null">
				#{phone,jdbcType=VARCHAR},
			</if>
			<if test="dayfen != null">
				#{dayfen,jdbcType=INTEGER},
			</if>
			<if test="registrationtime != null">
				#{registrationtime,jdbcType=VARCHAR},
			</if>
			<if test="name != null">
				#{name,jdbcType=VARCHAR},
			</if>
			<if test="age != null">
				#{age,jdbcType=INTEGER},
			</if>
			<if test="idcard != null">
				#{idcard,jdbcType=VARCHAR},
			</if>
			<if test="openId != null">
				#{openId,jdbcType=VARCHAR},
			</if>
		</trim>
	</insert>
	<update id="updateByPrimaryKeySelective"
		parameterType="com.zhita.model.manage.User">
		update user
		<set>
			<if test="sourceId != null">
				source = #{sourceId,jdbcType=INTEGER},
			</if>
			<if test="nickname != null">
				nickname = #{nickname,jdbcType=VARCHAR},
			</if>
			<if test="phone != null">
				phone = #{phone,jdbcType=VARCHAR},
			</if>
			<if test="dayfen != null">
				dayFen = #{dayfen,jdbcType=INTEGER},
			</if>
			<if test="registrationtime != null">
				registrationTime = #{registrationtime,jdbcType=VARCHAR},
			</if>
			<if test="name != null">
				name = #{name,jdbcType=VARCHAR},
			</if>
			<if test="age != null">
				age = #{age,jdbcType=INTEGER},
			</if>
			<if test="idcard != null">
				idCard = #{idcard,jdbcType=VARCHAR},
			</if>
			<if test="openId != null">
				openId = #{openId,jdbcType=VARCHAR},
			</if>
		</set>
		where id = #{id,jdbcType=INTEGER}
	</update>
	<update id="updateByPrimaryKey"
		parameterType="com.zhita.model.manage.User">
		update user
		set sourceId = #{sourceId,jdbcType=INTEGER},
		nickname = #{nickname,jdbcType=VARCHAR},
		phone =
		#{phone,jdbcType=VARCHAR},
		dayFen = #{dayfen,jdbcType=INTEGER},
		registrationTime = #{registrationtime,jdbcType=VARCHAR},
		name =
		#{name,jdbcType=VARCHAR},
		age = #{age,jdbcType=INTEGER},
		idCard =
		#{idcard,jdbcType=VARCHAR},
		openId = #{openId,jdbcType=VARCHAR}
		where id = #{id,jdbcType=INTEGER}
	</update>
	<!-- 查找用户表有多少条记录 -->
	<select id="getregistered" resultType="java.lang.Long">
		select count(openId) from
		user
	</select>
	<!-- 查找日新增用户数 -->
	<select id="getdailyUsers" resultType="java.lang.Long"
		parameterType="java.lang.Long">
		select count(openId) from user where
		registrationTime>=#{todayZeroTimestamps}
		and registrationTime &lt;
		#{tomorrowZeroTimestamps}
	</select>
	<!-- 查找月新增用户数 -->
	<select id="getmonthlyUsers" resultType="java.lang.Long"
		parameterType="java.lang.Long">
		select count(openId) from user where
		registrationTime>=#{monthlyZeroTimestamps} and registrationTime &lt;
		#{nextMonthlyZeroTimestamps}
	</select>

	  	<!--判断用户名是否存在 -->
	<select id="findFormatByLoginName" resultMap="BaseResultMap"
		parameterType="String">
    	<![CDATA[
       SELECT a.* FROM user a WHERE 1=1 
       AND a.phone=#{phone}
       AND a.openId=#{openId} 
       ]]>
	</select>
	  	<!-- 插入用户信息 -->
	<insert id="insertfootprint" parameterType="java.util.Map">
		insert into user (phone, nickname,
		openId,registrationTime)
		values (#{phone},
		#{nickName},
		#{openId},
		#{registrationTime})
	</insert>
	<!-- 后台管理   查询出用户表总数量 -->
	<select id="pageCount" resultType="java.lang.Integer">
		select count(openId) from user
	</select>
	<!-- 后台管理   通过手机号模糊查询用户总数量 -->
	<select id="pageCountByPhone" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user where phone like "%"#{phone}"%"
	</select>	
	<!--后台管理    通过渠道名称模糊查询出用户总数量  -->
	<select id="pageCountBySourceName" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user u inner join source s on u.sourceId=s.id where sourceName=#{sourceName}
	</select>
	<!-- 后台管理  通过注册时间模糊查询出用户总数量 -->
	<select id="pageCountByRegistrationtime" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user where registrationTime between #{arg0} and #{arg1}
	</select>
	<!-- 后台管理    通过手机号和渠道名称模糊查询用户总数量  -->
	<select id="pageCountByPhoneAndSourceName" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user u inner join source s on u.sourceId=s.id where u.phone like "%"#{arg0}"%" and s.sourceName=#{arg1}
	</select>
	<!-- 后台管理    通过手机号和注册时间模糊查询用户总数量  -->
	<select id="pageCountByPhoneAndRegistrationtime" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user where phone like "%"#{arg0}"%" and registrationTime between #{arg1} and #{arg2}
	</select>
	<!-- 后台管理    通过渠道名称和注册时间模糊查询用户总数量  -->
	<select id="pageCountBySourceNameAndRegistrationtime" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user u inner join source s on u.sourceId=s.id where s.sourceName=#{arg0} and u.registrationTime between #{arg1} and #{arg2}
	</select>
	<!-- 后台管理    通过手机号、渠道名称和注册时间模糊查询用户总数量  -->
	<select id="pageCountByPhoneSourceNameAndRegistrationtime" parameterType="java.lang.String" resultType="java.lang.Integer">
		select count(name) from user u inner join source s on u.sourceId=s.id where u.phone like "%"#{arg0}"%"　and s.sourceName=#{arg1} and u.registrationTime between #{arg2} and #{arg3}
	</select>
	<!-- 后台管理   根据用户id查询出按钮足迹  商品足迹和贷款分类足迹的总数量 -->
	<select id="pageCountThreeFootprint" parameterType="java.lang.Integer" resultType="java.lang.Integer">
		select count(userId) from 
		(select * from button_footprint b where userId=#{userId} and deleted='0'
		UNION ALL
		SELECT * from commodity_footprint c where userId=#{userId} and deleted='0'
		UNION ALL
		SELECT * from loan_classification_footprint l where userId=#{userId} and deleted='0') he
	</select>
	<resultMap type="com.zhita.model.manage.User" id="userSource">
	   <id property="id" column="id"/>
       <result property="nickname" column="nickname"/>         
       <result property="phone" column="phone"/> 
       <result property="dayfen" column="dayFen"/> 
       <result property="registrationtime" column="registrationTime"/> 
       <!-- ofType指定Source对象-->
       <collection property="source" ofType="com.zhita.model.manage.Source">
       		<result property="sourcename" column="sourceName"/>      
       </collection>
	</resultMap>
	
	<!-- 后台管理    查询出用户表所有信息，含分页 -->
	<select id="queryAllUser" parameterType="java.lang.Integer" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id limit #{page},2
	</select>
	<!-- 后台管理   通过手机号模糊查询，含分页 -->
	<select id="queryByPhone" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where u.phone like "%"#{arg0}"%" limit #{arg1},2
	</select>
	<!--后台管理   通过渠道名称模糊查询，含分页  -->
	<select id="queryBySourceName" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where s.sourceName=#{arg0} limit #{arg1},2
	</select>
	<!-- 后台管理  通过注册时间模糊查询，含分页 -->
	<select id="queryByRegistrationtime" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where u.registrationTime between #{arg0} and #{arg1} limit #{arg2},2
	</select>
	<!-- 后台管理   根据手机号和渠道名称进行模糊查询，含分页 -->
	<select id="queryByPhoneAndSourceName" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where u.phone like "%"#{arg0}"%" and s.sourceName=#{arg1} limit #{arg2},2
	</select>
	<!-- 后台管理   根据手机号和注册时间进行模糊查询，含分页 -->
	<select id="queryByPhoneAndRegistrationtime" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where u.phone like "%"#{arg0}"%" and u.registrationTime between #{arg1} and #{arg2} limit #{arg3},2
	</select>
	<!-- 后台管理   根据渠道名称和注册时间进行模糊查询，含分页 -->
	<select id="queryBySourceNameAndRegistrationtime" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where s.sourceName=#{arg0} and u.registrationTime between #{arg1} and #{arg2} limit #{arg3},2
	</select>
	<!-- 后台管理   根据手机号、渠道名称和注册时间进行模糊查询，含分页 -->
	<select id="queryByPhoneSourceNameAndRegistrationtime" resultMap="userSource">
		select s.sourceName,u.id,u.nickname,u.phone,u.dayFen,u.registrationTime from user u inner join source s on u.sourceId=s.id where u.phone like "%"#{arg0}"%" and s.sourceName=#{arg1} and u.registrationTime between #{arg2} and #{arg3} limit #{arg4},2
	</select>
	<!-- 后台管理 根据用户id查询出按钮足迹  商品足迹和贷款分类足迹    将其封装到按钮足迹实体类中  含分页-->
	<select id="queryAllButton" parameterType="java.lang.Integer" resultType="com.zhita.model.manage.ButtonFootprint">
		select * from button_footprint b where userId=#{arg0} and deleted='0'
		UNION ALL
		SELECT * from commodity_footprint c where userId=#{arg0} and deleted='0'
		UNION ALL
		SELECT * from loan_classification_footprint l where userId=#{arg0} and deleted='0'
		ORDER BY footprintTime desc limit #{arg1},2
	</select>

</mapper>